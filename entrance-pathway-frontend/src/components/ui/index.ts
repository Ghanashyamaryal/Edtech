// Re-export from atomic design structure for backward compatibility
export { Button, buttonVariants } from '../atoms';
export type { ButtonProps } from '../atoms';
export { Input } from '../atoms';
export type { InputProps } from '../atoms';
export { Label } from '../atoms';
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from '../molecules';
